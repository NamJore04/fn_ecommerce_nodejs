# ===================================
# 🚀 PRODUCTION ENVIRONMENT
# ===================================
# File này dùng cho môi trường PRODUCTION thực tế
# CRITICAL: Phải đổi tất cả secrets trước khi deploy!

# Server Configuration
NODE_ENV=production
PORT=5000

# Database Configuration (PRODUCTION)
# ⚠️ CHANGE THIS: Use your production MongoDB URI
MONGODB_URI=mongodb://admin:CHANGE_THIS_PASSWORD@your-mongo-host:27017/ecommerce?authSource=admin

# Redis Configuration (PRODUCTION)
# ⚠️ REQUIRED for production caching
REDIS_URL=redis://:CHANGE_THIS_PASSWORD@your-redis-host:6379

# Auto Import Data (DISABLE in production)
AUTO_IMPORT_DATA=false

# JWT Configuration
# ⚠️ CRITICAL: Generate new secure secrets!
JWT_SECRET=CHANGE_THIS_TO_RANDOM_STRING_MIN_32_CHARS
JWT_REFRESH_SECRET=CHANGE_THIS_TO_DIFFERENT_RANDOM_STRING_MIN_32_CHARS
JWT_EXPIRE=15m
JWT_REFRESH_EXPIRE=7d

# Social Authentication
# ⚠️ Use production OAuth credentials
GOOGLE_CLIENT_ID=your-production-google-client-id
GOOGLE_CLIENT_SECRET=your-production-google-secret
FACEBOOK_APP_ID=your-production-facebook-app-id
FACEBOOK_APP_SECRET=your-production-facebook-secret

# Email Configuration
# ⚠️ Use production SMTP service (SendGrid, AWS SES, etc.)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=namhuynhfree@gmail.com
EMAIL_PASS=udpsozawdonmmufr

# File Upload Configuration
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads

# CORS Configuration
# ⚠️ Set your production domain
CLIENT_URL=https://yourdomain.com

# Rate Limiting (Stricter for production)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=50

# ===================================
# 📝 PRODUCTION CHECKLIST
# ===================================
# Before deploying to production:
# 
# [ ] Change all CHANGE_THIS_* values
# [ ] Generate new JWT secrets (32+ chars)
# [ ] Use production MongoDB cluster (with replica set)
# [ ] Enable Redis for caching
# [ ] Use production SMTP service
# [ ] Set production domain in CLIENT_URL
# [ ] Disable AUTO_IMPORT_DATA
# [ ] Enable HTTPS
# [ ] Set up monitoring (PM2, New Relic, etc.)
# [ ] Configure backup strategy
# [ ] Set up logging service
# [ ] Enable rate limiting
# [ ] Review security headers (Helmet config)
# [ ] Set up CDN for static assets
# [ ] Configure firewall rules
# [ ] Enable database backups
# ===================================
