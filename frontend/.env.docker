# ===================================
# 🐳 DOCKER ENVIRONMENT
# ===================================
# File này dùng khi build Docker image
# API calls sẽ đi qua Nginx proxy

# API Configuration (DOCKER - through Nginx)
# ⚠️ CRITICAL: Set to /api for Docker environment (relative path through Nginx)
REACT_APP_API_URL=/api

# WebSocket URL (DOCKER - through Nginx)
# Uses same host as frontend (window.location.origin)
REACT_APP_SOCKET_URL=

# Google OAuth
REACT_APP_GOOGLE_CLIENT_ID=

# Build optimization
GENERATE_SOURCEMAP=false
