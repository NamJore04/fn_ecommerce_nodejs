# ===================================
# 🔧 DEFAULT ENVIRONMENT (FALLBACK)
# ===================================
# File này là fallback khi không tìm thấy .env.[NODE_ENV]
# Priority: .env.local > .env.docker > .env.production > .env

# API Configuration
# ⚠️ Để trống để apiDetector.js tự động detect
# REACT_APP_API_URL=http://localhost:5000/api

# Google OAuth
REACT_APP_GOOGLE_CLIENT_ID=
